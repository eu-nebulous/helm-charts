apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebulous-bqa-server.fullname" . }}
  labels:
    {{- include "nebulous-bqa-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nebulous-bqa-server.selectorLabels" . | nindent 4 }}
