apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nebulous-bqa-server.fullname" . }}-test-connection"
  labels:
    {{- include "nebulous-bqa-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nebulous-bqa-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
