{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nebulous-workflow-executor.serviceAccountName" . }}
  labels:
    {{- include "nebulous-workflow-executor.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: {{ include "nebulous-workflow-executor.namespace" . | quote }}
  name: workflow-executor # DO NOT USE THIS ROLE, IT IS JUST AN EXAMPLE
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["GET", "LIST", "WATCH", "PATCH"]
- apiGroups: ["argoproj.io"]
  resources: ["workflows"]
  verbs: ["GET", "LIST", "WATCH", "UPDATE", "PATCH"]
- apiGroups: ["workflow.io"]
  resources: ["*"]
  verbs: ["*"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: workflow-executor # DO NOT USE THIS ROLE BINDING, IT IS JUST AN EXAMPLE
  namespace: argo
subjects:
- kind: ServiceAccount
  name: {{ include "nebulous-workflow-executor.serviceAccountName" . }}
  namespace: {{ include "nebulous-workflow-executor.namespace" . | quote }}
roleRef:
  kind: Role
  name: workflow-executor # DO NOT USE THIS ROLE, IT IS JUST AN EXAMPLE
  apiGroup: rbac.authorization.k8s.io
{{- end }}