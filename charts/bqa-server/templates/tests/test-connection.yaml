apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bqa-server.fullname" . }}-test-connection"
  labels:
    {{- include "bqa-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bqa-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
