apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nebulous-sla-generator.fullname" . }}-test-connection"
  labels:
    {{- include "nebulous-sla-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nebulous-sla-generator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
