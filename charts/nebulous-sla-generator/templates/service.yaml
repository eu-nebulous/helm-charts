apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebulous-sla-generator.fullname" . }}
  labels:
    {{- include "nebulous-sla-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nebulous-sla-generator.selectorLabels" . | nindent 4 }}
